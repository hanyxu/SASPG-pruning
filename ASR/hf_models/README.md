# Pretrained checkpoints

Weight files (`pytorch_model.bin`, etc.) are **not** stored in git.

From the repo root:

```bash
bash scripts/fetch_hf_models.sh
```

Required bundles for the default smoke grid: `wav2vec2-base`, `wav2vec2-base-100h`, `hubert-large-ll60k`.
